{
  "name": "cknerv-workspace",
  "private": true,
  "version": "1.1.0",
  "license": "GPL-3.0",
  "scripts": {
    "brand:assets": "node scripts/generate-brand-assets.mjs",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4",
    "vitest": "^2.0",
    "@types/node": "^20.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
